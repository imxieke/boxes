# Git
alias ga='git add'
alias gaa='git add --all'
alias gdp="git clone --depth=1"
alias git-ignore-files='git check-ignore -v'
# alias git-pull-all-dirs="for i in $(ls); do { if [ -d ${i}/.git ]; then git -C $i pull; } fi ;done"