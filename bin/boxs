#!/usr/bin/env bash
# Boxs Manager Script

case $1 in
	install )
		echo "Install"
		;;
	backup )
		echo "backup"
		;;
	 remove)
		echo "Remove"
		;;
	 init)
		echo "Init"
		;;
	*)
		echo "Unknown Options $@"
		;;
esac